for (i = 0; i < 16; i++) { 
    if (i % 2 === 0) { // using remainder operator to decide if it's even or odd.
        console.log(i + " is even"); // Putting space between the double quotes so that there's one space between the number and the string.
    } else {
        console.log(i + " is odd");
    }
}